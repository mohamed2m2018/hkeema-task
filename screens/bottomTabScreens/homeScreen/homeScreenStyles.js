import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  QuestionText: {
    fontSize: 20,
    marginTop: 20,
    marginBottom: 20,
    color: '#4FB7B3',
    paddingLeft: 10,
    fontWeight: 'bold',
  },
});
